{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "-   Working with APIs (Useing OpenWeatherMap and GeoNames)\r -   JSON parsing (using libraries like Newtonsoft.Json)\r -   Asynchronous programming (to fetch data without blocking the UI)\r -   Add local storage to save user's preferred locations\r -   Implement a 5-day forecast view\r -   Add temperature unit conversion (C° to F°)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.9"
  }
}
